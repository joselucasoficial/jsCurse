function find(nums){
    let max_num = number.NEGATIVE_INFINITY
    // menor que todos os numeros
    for(let num of nums){
        if(num > max_num){
            
        }
    }
    return max_num
}